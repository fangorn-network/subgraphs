# Mono-repo for Subraphs

1. `client`: Subgraph client that includes standard queries
2. `client-types`: Vanilla typescript types that are returned by the subgraph client
3. `subgraph`: The subgraph implementations (not a part of the pnpm workspace)